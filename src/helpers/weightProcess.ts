export const f_WeightProcess = ( pIterations: number ): string => {
    for (let index = 0; index < pIterations ; index++) {
        //const element = array[index];

        console.log('Here we go '+index);
    }
    
    return `${ pIterations } iterations done`;
}